{
    "name": "flightradar24-card",
    "version": "1.0.0",
    "type": "module",
    "description": "Home Assistant custom card for Flightradar24 integration",
    "devDependencies": {
        "@eslint/js": "^9.0.0",
        "@playwright/browser-chromium": "^1.58.2",
        "@vitest/browser": "^4.1.2",
        "@vitest/browser-playwright": "^4.1.2",
        "@vitest/coverage-v8": "^4.0.0",
        "concurrently": "^9.2.1",
        "esbuild": "^0.27.5",
        "eslint": "^9.0.0",
        "globals": "^16.0.0",
        "jsdom": "^26.0.0",
        "playwright": "^1.58.2",
        "puppeteer": "^24.40.0",
        "tslib": "^2.8.0",
        "typescript": "^5.7.0",
        "vite": "^8.0.3",
        "vitest": "^4.0.0"
    },
    "scripts": {
        "fetch-runway-data": "node scripts/fetch-runway-data.js",
        "prebuild": "pnpm run fetch-runway-data",
        "build": "vite build",
        "build:watch": "vite build --watch",
        "dev": "vite",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage",
        "test:browser": "vitest run --config vitest.browser.config.ts",
        "test:browser:watch": "vitest --config vitest.browser.config.ts",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "typecheck": "tsc --noEmit"
    }
}
