name: CI — Build (pnpm)

# Run on pushes to main, PRs, tag pushes (so tag-based releases build), and manual dispatch
on:
  push:
    branches: [ main ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# Prevent duplicate concurrent runs for the same ref
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build, zip and upload artifact
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js 20 and pnpm cache
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies (pnpm)
        run: pnpm install --frozen-lockfile

      - name: Read package.json into output (for conditional steps)
        id: pkg
        run: |
          if [ -f package.json ]; then
            echo "pkg<<EOF" >> $GITHUB_OUTPUT
            cat package.json >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "pkg<<EOF" >> $GITHUB_OUTPUT
            echo "{}" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Run lint (if configured)
        if: contains(steps.pkg.outputs.pkg, '"lint"')
        run: pnpm lint
        continue-on-error: true

      - name: Run tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Prepare release artifact
        run: |
          mkdir -p release-artifact
          if [ -d "dist" ]; then
            zip -r release-artifact/home-assistant-flightradar24-card.zip dist hacs.json LICENSE README.md
          else
            echo "dist directory not found — nothing to zip" && exit 1
          fi

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: home-assistant-flightradar24-card
          path: release-artifact/home-assistant-flightradar24-card.zip
          retention-days: 7